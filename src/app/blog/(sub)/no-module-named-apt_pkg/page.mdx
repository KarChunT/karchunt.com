---
title: No module named APT-PKG
description: Learn how to solve the error "No module named APT-PKG" in Python.
date: 2025-03-28
author: KarChunT
tags: [Python]
---

## Steps to solve the error "No module named APT-PKG"

### Method 1 - Install `python-apt` package

```bash
sudo apt-get install python-apt
```

### Method 2 - Reinstall `python3-apt` package

```bash
sudo apt-get python3-apt --reinstall
```

### Method 3 - Properly set `.so` file location

```bash
cd /usr/lib/python3/dist-packages
sudo cp apt_pkg.cpython-310-x86_64-linux-gnu.so apt_pkg.so
```
<Comments />

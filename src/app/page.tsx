'use client';

import Hero from '@/components/shared/home/hero';
import CoreValues from '@/components/shared/home/coreValues';

const page = () => {
  return (
    <div>
      <Hero />
      <CoreValues />

      {/* title */}
      {/* principles or values blending seamlessly together, amplifying each other’s strengths. */}
    </div>
  );
};

export default page;
